{
  "name": "hapi-phillipclapham",
  "version": "0.3.0",
  "description": "Human API - Phill Clapham",
  "type": "module",
  "scripts": {
    "build": "node src/build.js",
    "build-worker": "node src/build-worker.js",
    "deploy": "npm run build && npm run build-worker && wrangler deploy",
    "dev": "npm run build-worker && wrangler dev",
    "update": "node src/build.js && git add . && git commit -m 'Update status' && git push"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
